<section class="manifesto" aria-labelledby="manifesto-heading">
  <div class="manifesto-inner reveal-up">
    <h2 id="manifesto-heading">
      De sterkste websites zijn niet alleen mooi.
      <span>Ze vertellen een verhaal, sturen op gevoel en maken kiezen vanzelfsprekend.</span>
    </h2>
    <p>
      Daarom verbind ik strategie, design en development tot één ervaring die klopt—voor je merk, je
      bezoeker en je volgende stap.
    </p>
  </div>
</section>

<style>
  .manifesto {
    padding: clamp(7.5rem, 14vw, 13rem) 1.25rem;
    background: #ffffff;
  }

  .manifesto-inner {
    max-width: 71rem;
    margin-inline: auto;
    text-align: center;
  }

  h2 {
    color: #111111;
    font-size: clamp(2.55rem, 5.2vw, 5.75rem);
    font-weight: 570;
    letter-spacing: -0.06em;
    line-height: 1;
    text-wrap: balance;
  }

  h2 span {
    display: block;
    color: #9a9a94;
  }

  p {
    max-width: 38rem;
    margin: 2.2rem auto 0;
    color: #565652;
    font-size: clamp(1rem, 1.5vw, 1.2rem);
    line-height: 1.6;
    text-wrap: pretty;
  }

  @media (max-width: 640px) {
    .manifesto {
      padding-block: 7rem;
    }

    h2 {
      text-align: left;
    }

    p {
      text-align: left;
    }
  }
</style>
